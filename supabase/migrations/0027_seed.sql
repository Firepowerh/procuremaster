-- Seed data for LOCAL DEVELOPMENT ONLY.
-- Do NOT push this to production.
-- Creates one test org with PM, DH, FA, two vendors, one RFP, one requirement.

-- Note: auth.users rows are created via supabase auth admin API in seed.sql
-- These SQL inserts assume the auth users already exist with known UUIDs.
-- Use supabase/seed.sql (root) for the full seed with auth user creation.
-- This migration file is intentionally minimal — full seed is in supabase/seed.sql.

SELECT 'Seed migration placeholder — full seed data is in supabase/seed.sql' AS note;
